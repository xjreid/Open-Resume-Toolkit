{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "overlay",
  "description": "Minimum core permissions for the local M0 overlay window.",
  "windows": ["overlay"],
  "permissions": ["core:default"]
}
