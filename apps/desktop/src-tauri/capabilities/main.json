{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main",
  "description": "Minimum core permissions for the local M0 main window.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
