# Desktop application implementation plans — reserved

Future plans in this folder should cover:

- Framework and Windows/macOS architecture support
- Application lifecycle, windows, tray/menu behavior, and real desktop overlay
- Structured resume editor, draft/publish experience, tracker, AI Activity, settings, and local search
- Mutually exclusive No AI, Direct API, and Codex subscription setup; vault integration; managed sign-in; provider/model comparison; and explicit switching
- Local operation orchestration, cancellation, crash recovery, and progress
- Accessible activity summaries, per-model/provider token and cost tables, filters, attempt details, direct/Codex model controls, estimate/quota explanations, spend and quota-cap controls, local notifications, CSV/JSON export, retention, activity clearing, and separate cap resets
- Rendering preview integration and safe local export/drag behavior
- Browser-connection status and repair interface
- Backup, restore, deletion, and storage-management interface
- Update-channel detection and Check for updates behavior
- Native accessibility, keyboard navigation, screen readers, scaling, and reduced motion

Do not design visual styling here; component appearance belongs under `../../Aesthetic/`.
