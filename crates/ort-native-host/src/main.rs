//! Browser native-messaging host placeholder. Production registration is deferred.

fn main() {
    eprintln!("Browser native messaging is disabled until the M5 security gate passes.");
    std::process::exit(78);
}
